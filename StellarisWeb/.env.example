PORT=3001
HOST=0.0.0.0
# Production: restrict new WebSocket connections to your website origin.
# ALLOWED_ORIGIN=https://your-domain.example

# Optional browser-lab backend. Default is the local SpacetimeDB instance.
# VITE_SPACETIME_URI=ws://127.0.0.1:3100
# CLI/load tools can target a separately provisioned test server:
# SPACETIME_HTTP=http://127.0.0.1:3100
# SPACETIME_WS=ws://127.0.0.1:3100
